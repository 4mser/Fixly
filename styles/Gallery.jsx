import styled from "styled-components";

export const Gallery = styled.div`
    margin: 10%;
    display: flex;
    justify-content: space-around;
    align-items: center;
    flex-wrap: wrap;
    gap: 2rem;
`